server:
  port: ${port:5501}
eureka:
  client:
    serviceUrl:
      defaultZone: ${url.registry:http://registry:1111/eureka/}
  instance:
    preferIpAddress: true
ribbon:
  eureka:
    enabled: true
