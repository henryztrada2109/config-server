server:
  port: ${port:5600}
  context-path: /v1

spring:
  application:
    name: seguridad
    profiles: docker
  datasource:
    url: jdbc:mysql://${url.db:localhost:3306/olimpo}
    username: ${url.db.username:root}
    password: ${url.db.password:root}
  liquibase:
    change-log: classpath:dbchangelog.xml
    enabled: true

  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    hibernate:
      ddl-auto: none
eureka:
  instance:
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 10
    ip-address: ${ipaddress:localhost}
    hostname: ${hostname:localhost}
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${url.registry:http://registry:1111/eureka/}
  
